import numToWords from 'ally/utils/num-to-words';
import { module, test } from 'qunit';

module('Unit | Utility | num to words');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numToWords();
  assert.ok(result);
});
